diff --git a/drivers/kernelsu/supercalls.c b/drivers/kernelsu/supercalls.c
index 1a2b3c4..5d6e7f8 100644
--- a/drivers/kernelsu/supercalls.c
+++ b/drivers/kernelsu/supercalls.c
@@ -8,6 +8,12 @@
 #include <linux/uaccess.h>
 #include <linux/slab.h>

+#ifdef CONFIG_KSU_SUSFS
+#include "susfs.h"
+#include "susfs_def.h"
+#endif
+
 static long ksu_supercall(unsigned int cmd, unsigned long arg)
 {
 	void __user *uarg = (void __user *)arg;
@@ -120,6 +126,7 @@ static long ksu_supercall(unsigned int cmd, unsigned long arg)
 #ifdef CONFIG_KSU_SUSFS
 	case KSU_SUSFS_ADD_SUS_PATH:
-		return susfs_add_sus_path(uarg);
+		return susfs_add_sus_path((struct st_susfs_sus_path __user *)uarg);

 	case KSU_SUSFS_DEL_SUS_PATH:
-		return susfs_del_sus_path(uarg);
+		return susfs_del_sus_path((struct st_susfs_sus_path __user *)uarg);

 	case KSU_SUSFS_ADD_SUS_MOUNT:
-		return susfs_add_sus_mount(uarg);
+		return susfs_add_sus_mount((struct st_susfs_sus_mount __user *)uarg);

 	case KSU_SUSFS_DEL_SUS_MOUNT:
-		return susfs_del_sus_mount(uarg);
+		return susfs_del_sus_mount((struct st_susfs_sus_mount __user *)uarg);

 	case KSU_SUSFS_ADD_SUS_KSTAT:
-		return susfs_add_sus_kstat(uarg);
+		return susfs_add_sus_kstat((struct st_susfs_sus_kstat __user *)uarg);

 	case KSU_SUSFS_DEL_SUS_KSTAT:
-		return susfs_del_sus_kstat(uarg);
+		return susfs_del_sus_kstat((struct st_susfs_sus_kstat __user *)uarg);

 	case KSU_SUSFS_SET_I_STATE_EXTERNAL_DIR:
-		return susfs_set_i_state_on_external_dir(uarg);
+		return susfs_set_i_state_on_external_dir((char __user *)uarg, cmd);

 	case KSU_SUSFS_CLR_I_STATE_EXTERNAL_DIR:
-		return susfs_clr_i_state_on_external_dir(uarg);
+		return susfs_clr_i_state_on_external_dir((char __user *)uarg, cmd);
 #endif
 	default:
 		return -EINVAL;
