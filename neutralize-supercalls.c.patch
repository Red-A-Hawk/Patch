diff --git a/drivers/kernelsu/supercalls.c b/drivers/kernelsu/supercalls.c
index 0000000..1111111 100644
--- a/drivers/kernelsu/supercalls.c
+++ b/drivers/kernelsu/supercalls.c
@@ -286,47 +286,25 @@ static int do_manage_mark(void __user *arg)
 
 	switch (cmd.operation) {
 	case KSU_MARK_GET: {
-#ifndef CONFIG_KSU_SUSFS
 		ret = ksu_get_task_mark(cmd.pid);
 		if (ret < 0) {
 			pr_err("manage_mark: get failed for pid %d: %d\n",
 			       cmd.pid, ret);
 			return ret;
 		}
 		cmd.result = (u32)ret;
 		break;
-#else
-        if (susfs_is_current_proc_umounted()) {
-            ret = 0;
-        } else {
-            ret = 1;
-        }
-        pr_info("manage_mark: ret for pid %d: %d\n", cmd.pid, ret);
-        cmd.result = (u32)ret;
-        break;
-#endif
 	}
 	case KSU_MARK_MARK: {
-#ifndef CONFIG_KSU_SUSFS
 		if (cmd.pid == 0) {
 			ksu_mark_all_process();
 		} else {
 			ret = ksu_set_task_mark(cmd.pid, true);
 			if (ret < 0) {
@@ -336,12 +314,6 @@ static int do_manage_mark(void __user *arg)
 			}
 		}
-#else
-        if (cmd.pid != 0) {
-            return ret;
-        }
-#endif
 		break;
 	}
 	case KSU_MARK_UNMARK: {
-#ifndef CONFIG_KSU_SUSFS
 		if (cmd.pid == 0) {
 			ksu_unmark_all_process();
 		} else {
@@ -351,12 +323,6 @@ static int do_manage_mark(void __user *arg)
 			}
 		}
-#else
-        if (cmd.pid != 0) {
-            return ret;
-        }
-#endif
 		break;
 	}
 	case KSU_MARK_REFRESH: {
-#ifndef CONFIG_KSU_SUSFS
 		ksu_mark_running_process();
 		pr_info("manage_mark: refreshed running processes\n");
-#else
-        pr_info("susfs: cmd: KSU_MARK_REFRESH: do nothing\n");
-#endif
 		break;
 	}
