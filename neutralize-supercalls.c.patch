diff --git a/drivers/kernelsu/supercalls.c b/drivers/kernelsu/supercalls.c
--- a/drivers/kernelsu/supercalls.c
+++ b/drivers/kernelsu/supercalls.c
@@ -46,10 +46,6 @@
 #include "manual_su.h"
 #endif
 
-#ifdef CONFIG_KSU_SUSFS
-bool susfs_is_boot_completed_triggered __read_mostly = false;
-#endif
-
 bool ksu_uid_scanner_enabled = false;
 
 static int do_report_event(void __user *arg)
@@ -142,9 +138,6 @@ static int do_report_event(void __user *arg)
 			pr_info("boot_complete triggered\n");
 			on_boot_completed();
-#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
-			susfs_is_boot_completed_triggered = true;
-#endif
 		}
 		break;
 	}
@@ -300,7 +296,7 @@ static int do_manage_mark(void __user *arg)
 {
-#if defined(CONFIG_KSU_SYSCALL_HOOK) || defined(CONFIG_KSU_SUSFS)
+#ifdef CONFIG_KSU_SYSCALL_HOOK
 	struct ksu_manage_mark_cmd cmd;
 	int ret = 0;
 
@@ -318,14 +314,6 @@ static int do_manage_mark(void __user *arg)
 			return ret;
 		}
-#else
-		ret = susfs_is_current_proc_umounted() ? 0 : 1;
-		cmd.result = (u32)ret;
-		break;
-#endif
 		cmd.result = (u32)ret;
 		break;
 	}
@@ -410,45 +398,24 @@ static int ksu_handle_fd_request(void __user *arg)
 }
 
 int ksu_handle_sys_reboot(int magic1, int magic2, unsigned int cmd,
-			  void __user **arg)
+			  void __user **arg)
 {
 	if (magic1 != KSU_INSTALL_MAGIC1)
 		return 0;
 
-#ifdef CONFIG_KSU_SUSFS
-	if (magic2 == SUSFS_MAGIC && current_uid().val == 0)
-		return 0;
-#endif
-
 	if (magic2 == KSU_INSTALL_MAGIC2) {
 		void __user *argp = (void __user *)*arg;
 		if (IS_ERR(argp))
 			return 0;
 		return ksu_handle_fd_request(argp);
 	}
-
 	return 0;
 }
