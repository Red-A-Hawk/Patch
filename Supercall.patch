cat > Supercall.patch << 'ENDPATCH'
--- a/drivers/kernelsu/supercalls.c
+++ b/drivers/kernelsu/supercalls.c
@@ -927,79 +927,96 @@ static int ksu_handle_fd_request(void __user *arg)
 #else
 int ksu_handle_sys_reboot(int magic1, int magic2, unsigned int cmd,
 			  void __user **arg)
 {
+	void __user *argp;
+	
 	if (magic1 != KSU_INSTALL_MAGIC1) {
 		return -EINVAL;
 	}
 
+	argp = (void __user *)*arg;
+	if (IS_ERR(argp)) {
+		pr_err("Failed to deref user arg, err: %lu\n", PTR_ERR(argp));
+		return 0;
+	}
+
 	if (magic2 == SUSFS_MAGIC && current_uid().val == 0) {
 #ifdef CONFIG_KSU_SUSFS_SUS_PATH
 		if (cmd == CMD_SUSFS_ADD_SUS_PATH) {
-			susfs_add_sus_path(arg);
+			susfs_add_sus_path((struct st_susfs_sus_path* __user)argp);
 			return 0;
 		}
 		if (cmd == CMD_SUSFS_ADD_SUS_PATH_LOOP) {
-			susfs_add_sus_path_loop(arg);
+			susfs_add_sus_path_loop((struct st_susfs_sus_path* __user)argp);
 			return 0;
 		}
 		if (cmd == CMD_SUSFS_SET_ANDROID_DATA_ROOT_PATH) {
-			susfs_set_i_state_on_external_dir(arg);
+			susfs_set_i_state_on_external_dir((char __user*)argp, cmd);
 			return 0;
 		}
 		if (cmd == CMD_SUSFS_SET_SDCARD_ROOT_PATH) {
-			susfs_set_i_state_on_external_dir(arg);
+			susfs_set_i_state_on_external_dir((char __user*)argp, cmd);
 			return 0;
 		}
-#endif //#ifdef CONFIG_KSU_SUSFS_SUS_PATH
+#endif
 #ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
 		if (cmd == CMD_SUSFS_HIDE_SUS_MNTS_FOR_ALL_PROCS) {
-			susfs_set_hide_sus_mnts_for_all_procs(arg);
+			pr_info("susfs: CMD_SUSFS_HIDE_SUS_MNTS_FOR_ALL_PROCS is deprecated\n");
 			return 0;
 		}
-#endif //#ifdef CONFIG_KSU_SUSFS_SUS_MOUNT
+#endif
 #ifdef CONFIG_KSU_SUSFS_SUS_KSTAT
 		if (cmd == CMD_SUSFS_ADD_SUS_KSTAT) {
-			susfs_add_sus_kstat(arg);
+			susfs_add_sus_kstat((struct st_susfs_sus_kstat* __user)argp);
 			return 0;
 		}
 		if (cmd == CMD_SUSFS_UPDATE_SUS_KSTAT) {
-			susfs_update_sus_kstat(arg);
+			susfs_update_sus_kstat((struct st_susfs_sus_kstat* __user)argp);
 			return 0;
 		}
 		if (cmd == CMD_SUSFS_ADD_SUS_KSTAT_STATICALLY) {
-			susfs_add_sus_kstat(arg);
+			susfs_add_sus_kstat((struct st_susfs_sus_kstat* __user)argp);
 			return 0;
 		}
-#endif //#ifdef CONFIG_KSU_SUSFS_SUS_KSTAT
+#endif
 #ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME
 		if (cmd == CMD_SUSFS_SET_UNAME) {
-			susfs_set_uname(arg);
+			susfs_set_uname((struct st_susfs_uname* __user)argp);
 			return 0;
 		}
-#endif //#ifdef CONFIG_KSU_SUSFS_SPOOF_UNAME
+#endif
 #ifdef CONFIG_KSU_SUSFS_ENABLE_LOG
 		if (cmd == CMD_SUSFS_ENABLE_LOG) {
-			susfs_enable_log(arg);
+			bool enabled;
+			if (copy_from_user(&enabled, argp, sizeof(bool)) == 0)
+				susfs_set_log(enabled);
 			return 0;
 		}
-#endif //#ifdef CONFIG_KSU_SUSFS_ENABLE_LOG
+#endif
 #ifdef CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
 		if (cmd == CMD_SUSFS_SET_CMDLINE_OR_BOOTCONFIG) {
-			susfs_set_cmdline_or_bootconfig(arg);
+			susfs_set_cmdline_or_bootconfig((char* __user)argp);
 			return 0;
 		}
-#endif //#ifdef CONFIG_KSU_SUSFS_SPOOF_CMDLINE_OR_BOOTCONFIG
+#endif
 #ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
 		if (cmd == CMD_SUSFS_ADD_OPEN_REDIRECT) {
-			susfs_add_open_redirect(arg);
+			susfs_add_open_redirect((struct st_susfs_open_redirect* __user)argp);
 			return 0;
 		}
-#endif //#ifdef CONFIG_KSU_SUSFS_OPEN_REDIRECT
+#endif
 #ifdef CONFIG_KSU_SUSFS_SUS_MAP
 		if (cmd == CMD_SUSFS_ADD_SUS_MAP) {
-			susfs_add_sus_map(arg);
+			susfs_add_sus_map((struct st_susfs_sus_map* __user)argp);
 			return 0;
 		}
-#endif // #ifdef CONFIG_KSU_SUSFS_SUS_MAP
+#endif
 		if (cmd == CMD_SUSFS_ENABLE_AVC_LOG_SPOOFING) {
-			susfs_set_avc_log_spoofing(arg);
+			bool enabled;
+			if (copy_from_user(&enabled, argp, sizeof(bool)) == 0)
+				susfs_set_avc_log_spoofing(enabled);
 			return 0;
 		}
 		if (cmd == CMD_SUSFS_SHOW_ENABLED_FEATURES) {
-			susfs_get_enabled_features(arg);
+			susfs_get_enabled_features((char __user*)argp, 4096);
 			return 0;
 		}
 		if (cmd == CMD_SUSFS_SHOW_VARIANT) {
-			susfs_show_variant(arg);
+			pr_info("susfs: SUSFS_VARIANT=%s\n", SUSFS_VARIANT);
 			return 0;
 		}
 		if (cmd == CMD_SUSFS_SHOW_VERSION) {
-			susfs_show_version(arg);
+			pr_info("susfs: SUSFS_VERSION=%s\n", SUSFS_VERSION);
 			return 0;
 		}
 		return 0;
 	}
 
-	void __user *argp = (void __user *)*arg;
-	if (IS_ERR(argp)) {
-		pr_err("Failed to deref user arg, err: %lu\n", PTR_ERR(argp));
-		return 0;
-	}
-
 	if (magic2 == KSU_INSTALL_MAGIC2) {
 		return ksu_handle_fd_request(argp);
 	}
 	
-    return 0;
+	return 0;
 }
-#endif // #ifndef CONFIG_KSU_SUSFS
+#endif
ENDPATCH
